# runelitemultilauncher
a python based launcher that launches multiple instances of runelite at once for convenient multi logging.
Place the .py file in the RuneLite Directory
default username is:

admin

password is:

default
